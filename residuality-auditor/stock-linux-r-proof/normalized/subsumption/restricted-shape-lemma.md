# Restricted Shape Lemma

Result: `RESTRICTED_SUBSUMPTION_ESTABLISHED`

This lemma is restricted to kernel `6.17.0-35-generic`, exact comparison mode `0`, frontier `41`, and the captured State V2 witness shape. It does not claim general Linux `states_equal` semantics.

Modeled relation: `gamma_l(current) subseteq gamma_l(retained)` for the local future-observable fields represented in this proof bundle.
