# Local Concretization

- `a=0`: `SIGMA_A0_IN_DIRECT_GAMMA` using `current` state
- `a=1`: `SIGMA_A1_IN_DIRECT_GAMMA` using `retained` state

Joint coverage: `JOINT_COVERAGE_CANDIDATE`

Each membership file contains per-register, range, tnum, stack, frame, and environment checks.
