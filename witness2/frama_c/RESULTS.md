# Frama-C EVA — corrected-model and historical results

## Current corrected model

`nand_mod.c` now uses two independent calls to `Frama_C_interval(0,1)` for
`a` and `b`. It was analyzed successfully on the artifact VM:

- Tool: Frama-C 25.0-beta (Manganese), EVA value analysis.
- Host: Ubuntu 24.04.3, Linux 6.17.0-35-generic, aarch64.
- Command: `frama-c -eva -eva-slevel 0 nand_mod.c` via `bash run.sh`.
- Current log: `out/eva_slevel0.current.log`.
- Provenance: `out/current.provenance.json`, which binds this source/log/version
  to `results/env.json` and eBPF run ID
  `20260710T044401Z-604a5721-8db0-4681-b46f-55fafb354c20`.
- Current-log SHA-256:
  `6d496d5685ced6d0d0a33bc4e7ff6b67648b6bc0d3250a970dd6dfc203611176`.
- Result: `NAND_out: {0; 1}`, `ABLATION_out: {1}`, and 0 alarms.

To create evidence for the corrected source on the artifact VM:

```sh
bash run.sh
```

The command regenerates `out/eva_slevel0.current.log` and preserves the
historical log. The current result establishes only global value ranges; it
does not certify an input/output relation or the toy relational inequality.

## Archived old-model result

- Status: captured successfully for the previous volatile-input source model.
- Tool: Frama-C 25.0-beta (Manganese), EVA value analysis.
- Command then used: `frama-c -eva -eva-slevel 0 nand_mod.c`.
- Host at capture time: Ubuntu 24.04.3, Linux 6.17.0-19-generic, aarch64.
- Unchanged log: `out/eva_slevel0.log`.
- SHA-256: `21fe208f9bb1e2b1df99a340f6f72d2ca002e031061106e9f3cb996d7184bcb5`.

The historical log contains:

```text
[eva] nand_mod.c:41: Frama_C_show_each_NAND_out: {0; 1}
[eva] nand_mod.c:44: Frama_C_show_each_ABLATION_out: {1}
...
[eva:final-states] Values at end of function nand3:
  acc ∈ {1; 2; 3}
[eva:final-states] Values at end of function nand7:
  acc ∈ {1; 2; 3}
...
  0 alarms generated by the analysis.
```

Those line locators refer to the old source revision. The corrected source
happens to keep the display calls on lines 41 and 44, but equal line numbers do
not establish source identity. This log must not be represented as output from
the current `Frama_C_interval` model.

## Conservative interpretation of the archived values

For the old model only, the log records global output range `{0,1}` for
modulo NAND and `{1}` for a different constant mod-7 program. It does not
support any of the following claims:

1. A modulo-specific precision contrast with equivalent explicit NAND.
2. An interval-completeness counterexample on the reachable set `X={1,2,3}`.
3. A row-indexed or other relational I/O certificate.
4. A same-function ablation: mod 7 changes the computed function.
5. System-independence from, or a causal analogy to, the Linux verifier.

The strict relation inequality in `../witness.py` belongs only to that toy
range-only `MOD` transfer and must not be attributed to Frama-C EVA.
